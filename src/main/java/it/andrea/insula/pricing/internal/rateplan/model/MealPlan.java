package it.andrea.insula.pricing.internal.rateplan.model;

/** Meal plan included in a rate plan. */
public enum MealPlan {
    ROOM_ONLY,
    BREAKFAST,
    HALF_BOARD,
    FULL_BOARD,
    ALL_INCLUSIVE
}